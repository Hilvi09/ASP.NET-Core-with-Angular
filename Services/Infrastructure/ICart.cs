using ASP.NET_Core_with_Angular.Models;

namespace ASP.NET_Core_with_Angular.Services.Infrastructure
{
    public interface ICart
    {/*
        IEnumerable<Cart> GetCarts();
        Cart GetCart(int id);

        void insert(Cart cart);
        void update(Category cart);
        void detele(int id);

        int Count();
        void Save();*/
    }
}
